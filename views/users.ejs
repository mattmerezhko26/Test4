<!doctype html>
<html>
  <head>
    <title>Users</title>
  </head>
  <body>
    <h1>Users</h1>
    <table border='1'>
      <tr>
        <th>ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Age</th>
      </tr>
      <% users.forEach(user => { %>
        <tr>
          <td><%= users.id %></td>
          <td><%= users.first_name %></td>
          <td><%= users.last_name %></td>
          <td><%= users.age %></td>
        </tr>
      <% }) %>
    </table>
  </body>
</html>
